# `@bna/eslint-config`
> Reusable config for Eslint

## Install
```sh
yarn add @bna/eslint-config -D
```

## Usage

```json
// package.json
{
  "eslintConfig": {
    "extends": [
      "@bna/eslint-config"
    ]
  },
}
```
