{
  "name": "bna",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "lerna run watch --parallel",
    "bootstrap": "lerna bootstrap && lerna run build --stream && lerna link",
    "publish": "lerna publish"
  },
  "prettier": {
    "parser": "typescript",
    "singleQuote": true,
    "semi": false,
    "trailingComma": "all",
    "tabWidth": 2
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
